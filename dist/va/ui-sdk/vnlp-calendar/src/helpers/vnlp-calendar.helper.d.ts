import { DateRange } from '../models/vnlp-calendar.model';
export declare const isSameDate: (date: Date, current: Date) => boolean;
export declare const createDateRange: (start: Date, end: Date) => DateRange;
